services:
  - type: web
    name: design-dropper-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm install -g dembrandt
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
